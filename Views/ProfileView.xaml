<UserControl x:Class="PonyvilleSchool2._0.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PonyvilleSchool2._0.Views"
             xmlns:voices="clr-namespace:PonyvilleSchool2._0.Services.Sounds"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Opacity" Value="0.4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track Name="PART_Track"
                           IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                          Background="Transparent"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                          Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="#A0A0A0"
                                   Margin="0,2"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
        
        <!-- ДОСТИЖЕНИЯ -->

        <Border Grid.Column="0"
                Background="White"
                CornerRadius="15"
                Margin="5">

            <StackPanel Margin="15">

                <TextBlock Text="🏆 Достижения"
                           FontSize="20"
                           FontWeight="Bold"/>

                <TextBlock Text="У тебя пока нет достижений"
                    FontSize="18"
                    TextAlignment="Center"
                    HorizontalAlignment="Center"
                    TextWrapping="Wrap"
                    Visibility="{Binding NotAchievements,
                        Converter={StaticResource BoolToVisibilityConverter}}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Achievements}" HorizontalAlignment="Left">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Left"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Border Width="100"
                            Height="100"
                            Margin="10"
                            Background="Transparent"
                            ToolTipService.InitialShowDelay="100">

                                    <Border.ToolTip>

                                        <StackPanel MaxWidth="250">

                                            <TextBlock Text="{Binding description}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,10"/>

                                            <TextBlock Text="{Binding received_at, StringFormat=dd.MM.yyyy HH:mm}"/>

                                        </StackPanel>

                                    </Border.ToolTip>
                                    <Border.RenderTransform>
                                        <ScaleTransform x:Name="scale"
                                ScaleX="1"
                                ScaleY="1"/>
                                    </Border.RenderTransform>

                                    <Border.Triggers>

                                        <EventTrigger RoutedEvent="MouseEnter">

                                            <BeginStoryboard>

                                                <Storyboard>

                                                    <DoubleAnimation
                                Storyboard.TargetName="scale"
                                Storyboard.TargetProperty="ScaleX"
                                To="1.1"
                                Duration="0:0:0.15"/>

                                                    <DoubleAnimation
                                Storyboard.TargetName="scale"
                                Storyboard.TargetProperty="ScaleY"
                                To="1.1"
                                Duration="0:0:0.15"/>

                                                </Storyboard>

                                            </BeginStoryboard>

                                        </EventTrigger>

                                        <EventTrigger RoutedEvent="MouseLeave">

                                            <BeginStoryboard>

                                                <Storyboard>

                                                    <DoubleAnimation
                                Storyboard.TargetName="scale"
                                Storyboard.TargetProperty="ScaleX"
                                To="1"
                                Duration="0:0:0.15"/>

                                                    <DoubleAnimation
                                Storyboard.TargetName="scale"
                                Storyboard.TargetProperty="ScaleY"
                                To="1"
                                Duration="0:0:0.15"/>

                                                </Storyboard>

                                            </BeginStoryboard>

                                        </EventTrigger>

                                    </Border.Triggers>

                                    <Grid>

                                        <Image Source="{Binding image_url}"
                       Stretch="UniformToFill"/>

                                    </Grid>

                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

        </Border>

        <!-- ИНФОРМАЦИЯ -->

        <Border Grid.Column="1" Grid.Row="2"
        Margin="5"
        CornerRadius="15"
        Background="#c3e8f4">

            <Grid>
                <!-- Содержимое профиля -->

                <!-- Кнопка "Назад "-->
                <Button Content="← Назад"
                Width="90"
                Height="35"
                Margin="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Command="{Binding BackCommand}"
                voices:VoiceAssist.Phrase="back"
                Grid.Row="1"/>

                <StackPanel Margin="20"
                        HorizontalAlignment="Center" Grid.Row="2">

                    <!-- Аватар -->
                    <Ellipse Width="150"
                         Height="150"
                         Margin="0,20,0,15">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding UserAvatarUrl}"
                                    Stretch="UniformToFill"/>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- ID и имя -->
                    <TextBlock FontSize="22"
                           FontWeight="Bold"
                           HorizontalAlignment="Center">
                    <Run Text="ID: "/>
                    <Run Text="{Binding UserId, Mode=OneWay}"/>
                    <Run Text=" | "/>
                    <Run Text="{Binding Username, Mode=OneWay}"/>
                    </TextBlock>

                    <!-- Уровень -->
                    <TextBlock
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="MediumPurple"
                           Margin="0,5,0,15"
                           HorizontalAlignment="Center">
                        <Run Text="Уровень: "/>
                        <Run Text="{Binding UserLevel, Mode=OneWay}"/>
                    </TextBlock>
                    <Separator Margin="0,10"/>

                    <!-- Выполнено -->
                    <TextBlock Text="📚 Выполнено заданий"
                           FontWeight="Bold"
                           FontSize="15"
                           HorizontalAlignment="Center"/>
                    <TextBlock
                           FontSize="18"
                           Margin="0,3,0,10"
                           HorizontalAlignment="Center">
                        <Run Text="Заданий: "/>
                        <Run Text="{Binding CompletedTasksCount, Mode=OneWay}"/>
                        <Run Text=" | Попыток: "/>
                        <Run Text="{Binding TotalCompletedTasks, Mode=OneWay}"/>
                    </TextBlock>

                    <!-- Любимый курс -->
                    <TextBlock Text="🍎 Любимый курс"
                           FontWeight="Bold"
                           FontSize="15"
                           HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding FavoriteCourse}"
                           FontSize="18"
                           Margin="0,3,0,10"
                           HorizontalAlignment="Center"/>
                    <Separator Margin="0,10"/>

                    <!-- Дата регистрации -->
                    <TextBlock Text="📅 В Ponyville School с"
                           FontWeight="Bold"
                           FontSize="15"
                           HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding RegistrationDate}"
                           FontSize="16"
                           Margin="0,3,0,20"
                           HorizontalAlignment="Center"/>
                    <!-- Кнопки -->
                    <Button Content="🚪 Выйти"
                        Width="180"
                        Height="35"
                        Margin="0,5"
                        Command="{Binding ExitCommand}"
                        voices:VoiceAssist.Phrase="exit"/>

                    <Button Content="🚪 Редактировать"
                        Width="180"
                        Height="35"
                        Margin="0,5"
                        Command="{Binding EditingCommand}"
                        voices:VoiceAssist.Phrase="editing"/>
                </StackPanel>

            </Grid>

        </Border>

        <!-- ИСТОРИЯ -->

        <Border Grid.Column="2"
                Background="White"
                Margin="5"
                CornerRadius="15">

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Шапка + сообщение о пустоте -->
                    <RowDefinition Height="*"/>
                    <!-- Список – займёт всё оставшееся место -->
                </Grid.RowDefinitions>

                <!-- Шапка и текст "Нет заданий" -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="📜 История"
                       FontSize="20"
                       FontWeight="Bold"/>
                    
                    <TextBlock Text="Ты не прошёл ещё ни одного задания"
                       TextWrapping="Wrap"
                       FontSize="18"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       Visibility="{Binding NotCompleted, 
                           Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Список заданий  -->
                <ListBox Grid.Row="1"
                 ItemsSource="{Binding CompletedTasks}"
                 BorderThickness="0"
                 Margin="2"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.ItemTemplate>

                        <DataTemplate>

                            <Border Margin="0,4"
                            Padding="10"
                            Background="#EEEEEE"
                            CornerRadius="15"
                            Width="293" Height="100">

                                <Grid>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Левая часть -->

                                    <StackPanel Grid.Column="0">

                                        <TextBlock Text="{Binding task_title}" TextWrapping="Wrap"
                                        FontWeight="Bold"
                                        FontSize="14"/>

                                        <TextBlock Text="{Binding course_title}"
                                        Foreground="Gray"
                                        Margin="0,2,0,2"/>

                                        <TextBlock Text="{Binding completed_date,
                                        StringFormat=dd.MM.yyyy HH:mm}"
                                        FontSize="11"/>

                                    </StackPanel>

                                    <!-- Правая часть -->

                                    <StackPanel Grid.Column="1"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">

                                        <TextBlock Text="{Binding score}"
                                        FontWeight="Bold"
                                        FontSize="18"
                                        HorizontalAlignment="Center"/>

                                        <Image Source="{Binding score_image_url}"
                                        Width="32"
                                        Height="32"/>

                                    </StackPanel>

                                </Grid>

                            </Border>

                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
