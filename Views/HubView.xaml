<UserControl x:Class="PonyvilleSchool2._0.Views.HubView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PonyvilleSchool2._0.ViewModels"
        xmlns:views="clr-namespace:PonyvilleSchool2._0.Views"
        mc:Ignorable="d">

    <UserControl.Resources>

        <DataTemplate DataType="{x:Type vm:CoursesViewModel}">
            <views:CoursesView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TasksViewModel}">
            <views:TasksView/>
        </DataTemplate>

    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Шапка-->
        <views:HeadView Grid.Row="0"/>

            <Border Grid.Row="1"
            Margin="10"
            CornerRadius="15"
            BorderBrush="#DDD"
            Background="White"
            BorderThickness="1">
            
            <!--Основная часть-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!--Левая часть-->
                <Border CornerRadius="15" ClipToBounds="True">
                    <ContentControl Grid.Column="0"
                            Content="{Binding CurrentView}"/>
                </Border>
                <!--Правая часть-->
            <views:MenuView Grid.Column="1"/>
        </Grid>
        </Border>
    </Grid>
</UserControl>