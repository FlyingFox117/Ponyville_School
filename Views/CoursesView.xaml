<UserControl x:Class="PonyvilleSchool2._0.Views.CoursesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid>
            <!-- Карта фоном -->
            <Rectangle RadiusX="15"
                       RadiusY="15">
                <Rectangle.Fill>
                    <ImageBrush ImageSource="/Assets/map.jpg"
                    Stretch="UniformToFill"/>
                </Rectangle.Fill>
            </Rectangle>
            <ItemsControl ItemsSource="{Binding Courses}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.SelectCourseCommand,
                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                    CommandParameter="{Binding}"
                    BorderThickness="0"
                    Padding="15"
                    FocusVisualStyle="{x:Null}"
                    Margin="20"
                    Width="220"
                    Height="220"
                    Background="Transparent"
                    ToolTipService.InitialShowDelay="100">

                            <Button.ToolTip>
                                <TextBlock>
        <Run Text="Прогресс: "/>
        <Run Text="{Binding completed_tasks}"/>
        <Run Text="/"/>
        <Run Text="{Binding total_tasks}"/>
                                </TextBlock>
                            </Button.ToolTip>
                            <Button.RenderTransform>
                                <ScaleTransform x:Name="scale" ScaleX="1" ScaleY="1"/>
                            </Button.RenderTransform>

                            <Button.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scale"
                                 Storyboard.TargetProperty="ScaleX"
                                 To="1.15" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="scale"
                                 Storyboard.TargetProperty="ScaleY"
                                 To="1.15" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>

                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scale"
                                 Storyboard.TargetProperty="ScaleX"
                                 To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="scale"
                                 Storyboard.TargetProperty="ScaleY"
                                 To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Button.Triggers>
                            <Grid>
                                <Image Source="{Binding house_image_url}"/>
                                <TextBlock Text="{Binding title}"
                               VerticalAlignment="Bottom"
                               Background="#80000000"
                               Foreground="White"
                               TextAlignment="Center"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border Background="#AAFFFFFF" ClipToBounds="True"
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Загрузка курсов..."
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
