<UserControl x:Class="PonyvilleSchool2._0.Views.Authentication.NewPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PonyvilleSchool2._0.Views.Authentication"
             xmlns:vm="clr-namespace:PonyvilleSchool2._0.ViewModels.Authentication"
             mc:Ignorable="d">
    
    <StackPanel Margin="30">
        <TextBlock Text="Новый пароль" FontSize="22" Margin="0 0 0 5"/>
        <!-- Сообщение пользователю -->
        <TextBlock
            Text="{Binding StatusMessage}"
            Foreground="{Binding StatusColor}"
            TextWrapping="Wrap"
            Margin="0 3 0 5"
            FontWeight="SemiBold"/>
        
        <TextBlock Text="Пароль"/>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Повторите пароль"/>
        <TextBox Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Content="Сохранить" Command="{Binding ConfirmCommand}" Margin="0 15 0 15" IsEnabled="{Binding IsNotLoading}"/>
        <Button Content="Отмена" Command="{Binding GoToLoginCommand}"/>
    </StackPanel>
</UserControl>
