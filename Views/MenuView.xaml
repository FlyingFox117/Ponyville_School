<UserControl x:Class="PonyvilleSchool2._0.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:b="clr-namespace:PonyvilleSchool2._0.Core"
             xmlns:voices="clr-namespace:PonyvilleSchool2._0.Services.Sounds"
             mc:Ignorable="d">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
           FontSize="24"
           FontWeight="Bold"
           Text="{Binding MenuTitle}"/>

        <TextBlock Grid.Row="1"
           TextWrapping="Wrap"
           FontSize="18"
           Text="{Binding MenuDescription}"/>

        <TextBlock Grid.Row="2"
               Margin="0 10 0 5"
               Text="{Binding ProgressText}" 
               FontSize="18" 
               TextAlignment="Center"/>

        <!-- Магия выглядывающей пони -->
        <Border Grid.Row="3" ClipToBounds="True">
            <Image HorizontalAlignment="Right" Stretch="Uniform" Margin="0,0,-50,0">
                <i:Interaction.Behaviors>
                    <b:ImageTransitionBehavior BoundSource="{Binding SelectedCourse.pony_image_url}" DurationSeconds="0.3" TransitionKind="FadeOutIn"/>
                </i:Interaction.Behaviors>
            </Image>
        </Border>

        <Button Grid.Row="4"
            Content="{Binding OpenButtonText}"
            Command="{Binding ToggleTasksCommand}"
            IsEnabled="{Binding IsCourseSelected}"
            voices:VoiceAssist.Phrase="{Binding ButtonPhrase}"
            Margin="0 20 0 0"/>
    </Grid>
</UserControl>