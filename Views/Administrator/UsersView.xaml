<UserControl x:Class="PonyvilleSchool2._0.Views.Administrator.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PonyvilleSchool2._0.Views.Administrator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <DataGrid ItemsSource="{Binding Users}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding login}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding username}"/>
                <DataGridTextColumn Header="XP" Binding="{Binding xp}"/>
                <DataGridTextColumn Header="Попыток" Binding="{Binding total_attempts}"/>
                <DataGridTextColumn Header="Средний балл" Binding="{Binding avg_score}"/>
                <DataGridTextColumn Header="Разных заданий" Binding="{Binding unique_tasks}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
