<Window x:Class="PonyvilleSchool2._0.Views.Administrator.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PonyvilleSchool2._0.ViewModels.Administrator"
        xmlns:views="clr-namespace:PonyvilleSchool2._0.Views"
        mc:Ignorable="d"
        Title="Администратор" Height="450" Width="800">
    
    <Window.DataContext>
        <vm:AdminWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <Grid Background="#2D2D30">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Верхние кнопки -->
            <StackPanel>
                <Button Content="Пользователи"
                Command="{Binding ShowUsersCommand}"
                Margin="10" Height="50"/>

                <Button Content="Курсы"
                Command="{Binding ShowAnalyticsCommand}"
                Margin="10" Height="50"/>
            </StackPanel>

            <!-- Кнопка выхода внизу -->
            <Button Grid.Row="1"
            Content="Выход"
            Margin="10"
            Height="45"
            Command="{Binding ExitCommand}"/>
        </Grid>

        <!-- Центр -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentView}"/>
    </Grid>
</Window>
